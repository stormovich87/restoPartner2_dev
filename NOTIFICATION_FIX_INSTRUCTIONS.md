# Исправление проблемы с уведомлениями в Telegram

## Проблема

Личные сообщения в Telegram не приходят при включенных настройках:
- Напоминания о смене
- Напоминание о закрытии смены
- Уведомления ответственным при не выходе сотрудника
- Уведомления кандидатам на замену

## Причины

Есть две основные причины, по которым боты не могут отправить личные сообщения:

### 1. Сотрудник не начал диалог с ботом (САМАЯ ЧАСТАЯ ПРИЧИНА)

**Ограничение Telegram:** Боты НЕ могут отправить первое сообщение пользователю. Пользователь должен сам начать диалог с ботом, отправив команду `/start`.

**Решение:**
1. Каждый сотрудник должен:
   - Найти бота в Telegram (по имени или через ссылку `t.me/ИМЯ_БОТА`)
   - Нажать кнопку "Start" или отправить команду `/start`
   - Пройти регистрацию в боте
2. После этого бот сможет отправлять личные уведомления

### 2. Сотрудник заблокировал бота

Если пользователь заблокировал бота, уведомления также не придут. Нужно разблокировать бота в настройках Telegram.

### 3. Не настроен токен бота

Проверьте, что в настройках партнёра установлен `employee_bot_token`.

### 4. У сотрудника не указан telegram_user_id

Сотрудник должен пройти регистрацию в боте, чтобы система получила его Telegram ID.

## Что было сделано

### 1. Добавлено детальное логирование

Обновлена edge function `shift-reminders` с подробным логированием всех попыток отправки сообщений:

**Логи помогут определить:**
- Попыток отправить сообщение и их результаты
- Если у сотрудника не указан telegram_user_id
- Если сотрудник не начал диалог с ботом (ошибка "chat not found")
- Если сотрудник заблокировал бота (ошибка "bot was blocked")

**Примеры логов:**
```
[SHIFT REMINDER] Shift abc123: sending before-shift reminder to Иван, telegram_id: 123456789
✓ Message sent successfully to 123456789

[SHIFT REMINDER] ✗ Failed to send before-shift reminder: chat not found
[SHIFT REMINDER] Employee has NOT STARTED the bot (no /start command)
```

### 2. Создан компонент диагностики

Добавлена новая кнопка "Диагностика уведомлений" в разделе "График работы":
- Проверяет настройки партнёра
- Проверяет токен бота
- Проверяет наличие telegram_user_id у сотрудников
- Показывает статистику отправленных уведомлений
- Объясняет причины проблем

## Как развернуть исправления

### Шаг 1: Обновите edge function

```bash
# Из корня проекта
npx supabase functions deploy shift-reminders
```

Это обновит функцию с улучшенным логированием.

### Шаг 2: Проверьте логи

После развертывания откройте Supabase Dashboard:
1. Перейдите в раздел "Edge Functions"
2. Выберите функцию `shift-reminders`
3. Откройте вкладку "Logs"
4. Дождитесь следующего выполнения функции (каждую минуту)
5. Изучите логи для определения точной причины проблемы

### Шаг 3: Используйте диагностику

В интерфейсе приложения:
1. Откройте раздел "График работы"
2. Нажмите на иконку 🩺 (Stethoscope) - "Диагностика уведомлений"
3. Нажмите "Запустить диагностику"
4. Изучите результаты

## Решение проблемы

### Если причина: сотрудник не начал диалог

1. Найдите токен вашего бота сотрудников в настройках
2. Получите имя бота через Telegram API или BotFather
3. Отправьте всем сотрудникам ссылку: `https://t.me/ИМЯ_БОТА`
4. Попросите каждого сотрудника:
   - Открыть ссылку
   - Нажать "Start"
   - Пройти регистрацию

### Если причина: не настроен токен бота

1. Откройте раздел "Сотрудники"
2. Нажмите на настройки бота сотрудников
3. Укажите токен бота
4. Включите бота

### Если причина: у сотрудников нет telegram_user_id

1. Убедитесь, что бот настроен
2. Попросите сотрудников пройти регистрацию в боте
3. После регистрации их telegram_user_id будет автоматически сохранён

## Проверка работы

После выполнения всех шагов:

1. Создайте тестовую смену для сотрудника, который прошёл все шаги
2. Установите время начала смены через несколько минут
3. Дождитесь времени отправки напоминания
4. Проверьте:
   - Получил ли сотрудник сообщение в Telegram
   - Логи в Supabase Dashboard
   - Результаты диагностики

## Дополнительная информация

### Таблицы для проверки

```sql
-- Проверить настройки партнёра
SELECT
  employee_bot_token,
  employee_bot_enabled,
  shift_reminders_enabled,
  shift_close_reminder_enabled,
  no_show_responsible_enabled,
  replacement_search_enabled
FROM partner_settings
WHERE partner_id = 'YOUR_PARTNER_ID';

-- Проверить сотрудников с telegram_user_id
SELECT
  id,
  first_name,
  last_name,
  telegram_user_id,
  is_active
FROM employees
WHERE partner_id = 'YOUR_PARTNER_ID'
  AND is_active = true;

-- Проверить попытки отправки уведомлений
SELECT
  id,
  date,
  reminder_before_sent_at,
  reminder_late_sent_at,
  close_reminder_sent_at,
  no_show_notified_at
FROM schedule_shifts
WHERE partner_id = 'YOUR_PARTNER_ID'
  AND date >= CURRENT_DATE - INTERVAL '7 days'
ORDER BY date DESC;
```

### Полезные ссылки

- [Telegram Bot API - sendMessage](https://core.telegram.org/bots/api#sendmessage)
- [Supabase Edge Functions Logs](https://supabase.com/docs/guides/functions/logging)
- [Telegram Bot Limitations](https://core.telegram.org/bots/faq#how-do-i-check-if-my-bot-is-blocked-by-someone)

## Поддержка

Если проблема не решена:
1. Изучите логи edge function
2. Проверьте результаты диагностики
3. Убедитесь, что все сотрудники начали диалог с ботом
4. Проверьте, что токен бота корректный и бот не заблокирован
