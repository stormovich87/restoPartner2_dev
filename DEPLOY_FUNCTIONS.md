# Развертывание Edge Functions

## Необходимо развернуть обновленные функции

Были обновлены следующие edge functions:
1. `shift-reminders` - с детальным логированием
2. `test-notification` - новая функция для тестирования уведомлений

## Способ 1: Через Supabase CLI (рекомендуется)

```bash
# Установите Supabase CLI, если еще не установлен
npm install -g supabase

# Войдите в Supabase
supabase login

# Разверните функции
supabase functions deploy shift-reminders
supabase functions deploy test-notification
```

## Способ 2: Через Supabase Dashboard

1. Откройте [Supabase Dashboard](https://supabase.com/dashboard)
2. Выберите ваш проект
3. Перейдите в раздел "Edge Functions"
4. Для каждой функции:
   - Нажмите "Deploy new version"
   - Скопируйте содержимое файла из `supabase/functions/[имя-функции]/index.ts`
   - Вставьте в редактор
   - Нажмите "Deploy"

### shift-reminders

Файл: `supabase/functions/shift-reminders/index.ts`

### test-notification

Файл: `supabase/functions/test-notification/index.ts`

## После развертывания

1. Откройте раздел "График работы" в вашем приложении
2. Нажмите на иконку 🩺 (Stethoscope) - "Диагностика уведомлений"
3. Нажмите "Запустить диагностику"
4. Если есть сотрудники с Telegram ID, вы увидите кнопки "Тест" рядом с каждым
5. Нажмите "Тест" для любого сотрудника
6. Если сотрудник начал диалог с ботом, он получит тестовое сообщение

## Проверка логов

После развертывания `shift-reminders`:

1. Откройте Supabase Dashboard
2. Перейдите в "Edge Functions" → "shift-reminders"
3. Откройте вкладку "Logs"
4. Дождитесь следующего выполнения (каждую минуту)
5. Изучите логи - теперь они покажут:
   - Какие сообщения пытается отправить система
   - Успешность отправки
   - Причины ошибок (если есть)

## Типичные ошибки в логах

### "chat not found"
**Причина:** Сотрудник не начал диалог с ботом
**Решение:** Попросите сотрудника найти бота в Telegram и нажать /start

### "bot was blocked"
**Причина:** Сотрудник заблокировал бота
**Решение:** Попросите сотрудника разблокировать бота в настройках Telegram

### "Unauthorized"
**Причина:** Неверный токен бота
**Решение:** Проверьте токен бота в настройках

## Проверка работы напоминаний о смене

1. Убедитесь, что функции развернуты
2. Откройте диагностику и отправьте тестовое уведомление
3. Если тест прошел успешно, создайте тестовую смену:
   - Время начала: через 5-10 минут
   - Сотрудник: тот, кому успешно отправилось тестовое уведомление
4. В настройках графика установите "Напоминание за" = 00:05 (5 минут)
5. Дождитесь времени напоминания
6. Проверьте:
   - Получил ли сотрудник напоминание в Telegram
   - Логи в Supabase Dashboard (должно быть "[SHIFT REMINDER] ✓ Before-shift reminder sent successfully")

## Важно

Telegram боты **НЕ МОГУТ** отправить первое сообщение пользователю. Каждый сотрудник должен:
1. Найти бота в Telegram (по имени или через ссылку `t.me/ИМЯ_БОТА`)
2. Нажать кнопку "Start" или отправить команду `/start`
3. Пройти регистрацию

Только после этого бот сможет отправлять личные уведомления.
